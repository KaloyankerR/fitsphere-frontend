import { Paper, Typography } from "@mui/material";

const Statistics = () => {
  return (
    <Paper elevation={3} sx={{ padding: 2 }}>
      <Typography variant="h6">Statistics</Typography>
      <Typography>Statistics data goes here.</Typography>
    </Paper>
  );
};

export default Statistics;
